'use client'
import Link from 'next/link'
import { ThemeToggle } from '@/components/theme-toggle'
export function AuthPage({mode='signin'}:{mode?:'signin'|'signup'|'forgot'|'reset'}){const isSignUp=mode==='signup';const isForgot=mode==='forgot';const isReset=mode==='reset';return <main className="min-h-screen bg-background px-5 py-8 text-foreground"><div className="mx-auto flex max-w-md flex-col items-center"><div className="flex w-full justify-end"><ThemeToggle/></div><Link href="/" className="mt-5 font-serif text-3xl">Heritage<span className="text-accent">.</span></Link><div className="mt-10 w-full rounded-3xl border border-border bg-card p-6 shadow-sm sm:p-8"><p className="text-xs uppercase tracking-[.22em] text-accent">{isSignUp?'Join Heritage Club':isForgot?'Account recovery':isReset?'Choose a new password':'Welcome back'}</p><h1 className="mt-3 font-serif text-4xl">{isSignUp?'Begin the journey.':isForgot?'Find your way back.':isReset?'Set a fresh password.':'Continue learning.'}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{isSignUp?'Create a family account and make heritage part of the week.':isForgot?'Enter your email and we will send recovery instructions.':isReset?'Your new password should be unique and easy for you to protect.':'Sign in to your Heritage Club learning space.'}</p>{!isReset&&<form className="mt-8 grid gap-4">{isSignUp&&<label className="grid gap-2 text-sm">Parent or guardian name<input className="h-11 rounded-xl border border-border bg-background px-3" placeholder="Your name"/></label>}<label className="grid gap-2 text-sm">Email address<input type="email" className="h-11 rounded-xl border border-border bg-background px-3" placeholder="you@example.com"/></label>{!isForgot&&<label className="grid gap-2 text-sm">Password<input type="password" className="h-11 rounded-xl border border-border bg-background px-3" placeholder="At least 8 characters"/></label>}<button className="mt-2 h-11 rounded-full bg-primary text-sm text-primary-foreground">{isSignUp?'Create account':isForgot?'Send recovery link':'Sign in'}</button></form>}{isReset&&<form className="mt-8 grid gap-4"><label className="grid gap-2 text-sm">New password<input type="password" className="h-11 rounded-xl border border-border bg-background px-3"/></label><label className="grid gap-2 text-sm">Confirm password<input type="password" className="h-11 rounded-xl border border-border bg-background px-3"/></label><button className="h-11 rounded-full bg-primary text-sm text-primary-foreground">Update password</button></form>}<div className="mt-6 text-center text-sm text-muted-foreground">{isSignUp?'Already have an account? ':isForgot||isReset?'Remember your password? ':'New to Heritage Club? '}<Link href={isSignUp||isForgot||isReset?'/auth':'/auth/signup'} className="text-foreground underline underline-offset-4">{isSignUp||isForgot||isReset?'Sign in':'Create an account'}</Link></div>{mode==='signin'&&<Link href="/auth/forgot-password" className="mt-3 block text-center text-xs text-muted-foreground underline">Forgot your password?</Link>}</div><p className="mt-6 text-center text-xs leading-5 text-muted-foreground">By continuing, you agree to our <Link href="/terms" className="underline">Terms</Link> and <Link href="/privacy" className="underline">Privacy Policy</Link>.</p></div></main>}
